version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Client-react
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: Client-react/dist
    files:
      - "**/*"
  cache:
    paths:
      - Client-react/node_modules/**/*
